defmodule ArrayMethods do
  @moduledoc """
  Examples of how to implement EcmaScript 6 array methods in Elixir
  """

  @doc """
  Hello world.

  ## Examples

      iex> ElixirEs6ArrayMethods.hello()
      :world

  """
  def map(array) do
    :example
  end

  def filter(array) do
    :example
  end

  def find(array) do
    :example
  end

  def findIndex(array) do
    :example
  end

  def some(array) do
    :example
  end

  def every(array) do
    :example
  end

  def reduce(array) do
    :example
  end
end
